interface:
  display_name: "GitHub正本の変更完了"
  short_description: "Git変更を安全に検証しcommitとremote照合"
  default_prompt: "$repository-change-workflow を使い、利用者が許可した範囲で変更を検証し、日本語commitと必要なremote照合まで完了する。"
policy:
  allow_implicit_invocation: true
